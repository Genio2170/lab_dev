# Requirements para módulos Utils
# Dependências necessárias para funcionalidades avançadas dos utilitários

# ===== DEPENDÊNCIAS CORE (OBRIGATÓRIAS) =====
requests>=2.31.0              # HTTP requests para APIs de IA

# ===== DEPENDÊNCIAS OPCIONAIS =====
# Descomente conforme necessário:

# Para funcionalidades de IA (ai_utils.py) - OPCIONAL
# openai>=1.0.0                 # SDK OpenAI - apenas se usar API oficial
# anthropic>=0.3.0              # SDK Claude - apenas se usar API oficial  

# Para validação avançada (validation.py) - OPCIONAL
# python-dateutil>=2.8.0        # Parse avançado de datas - nativo do Python já funciona

# Para logging estruturado (error_handler.py) - OPCIONAL
# python-json-logger>=2.0.0     # Logs em JSON - logging nativo já funciona

# Para processamento avançado de texto (helpers.py) - OPCIONAL
# python-slugify>=8.0.0         # Geração de slugs com Unicode - implementação própria já existe
# unidecode>=1.3.0              # Conversão Unicode - unicodedata nativo já funciona

# ===== DEPENDÊNCIAS JÁ INCLUÍDAS NO SISTEMA BASE =====
# Essas já devem estar instaladas no projeto principal:
# flask>=2.3.0
# werkzeug>=2.3.0

# ===== PARA DESENVOLVIMENTO E TESTES =====
pytest>=7.0.0                 # Framework de testes
pytest-cov>=4.0.0             # Coverage de testes